<html>

  <head>
    <title>PAGE POTIN</title>
  </head>
  
  <body>
    
    <h1>Voici la page du potin n°<%= params['id'] %></h1>
    <% id_page = params['id'] %>
    <% gossips.each_with_index do |gossip,id| %>
        <% if id == id_page.to_i %>
          <p>
            <strong>Auteur</strong>
            <br/><%= gossip.author %><br/>
          </p>
          <p>
            <strong>Contenu</strong>
            <br/><%= gossip.content %><br/>
          </p>

        <% end %>
    <% end %>

    <p><a href="/">Retour</a></p>
  </body>

</html>